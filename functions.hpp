#include <math.h>
#include <algorithm>
#include <string>
namespace funcs
{
	std::string getVertex(double a, double b, double c);
	std::string getDirection(double a);
	std::string getWidthPara(double a);
	double getAxisOfSym(double a, double b, double c);
	std::string getRoots(double a, double b, double c);
}